package com.example.onlineartstore.repository;

import com.example.onlineartstore.entity.WinningBet;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WinningBetRepository extends JpaRepository<WinningBet, Integer> {
}
